"use client";
const SubTag = () => {
  return (
    <div className="my-16 text-center animate-fade-in">
      <h2 className="text-sm text-zinc-500 ">
        Impossible is just a feature, not a bug.
      </h2>
    </div>
  );
};

export default SubTag;
